{
  "configurations": [
    
    {
      "type": "lldb",
      "request": "launch",
      "name": "Launch LTEngine",
      "program": "${workspaceFolder}/target/debug/ltengine",
      "args": [
        "--host",
        "0.0.0.0"
      ],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "cargo build"
    }
  ]
}